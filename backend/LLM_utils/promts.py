prompt_decompose_solution = '''
Ты — система разложения математического решения на атомарные шаги.

Твоя задача: разбить текст решения (`SOLUTION`) на последовательность элементарных, атомарных шагов.

---

### 🔒 Абсолютное правило №1 — *только дословные подстроки*:
Каждый шаг **должен быть точной, посимвольной подстрокой из `SOLUTION`**.
Это значит:
- нельзя менять ни одной буквы, ни пробела, ни знака препинания;
- нельзя добавлять точку, если в исходном тексте стояла запятая (и наоборот);
- нельзя убирать или добавлять кавычки, скобки, минусы, индексы, пробелы;
- нельзя ничего сокращать или переформулировать;
- нельзя объединять фрагменты из разных мест текста.

🚨 Никаких изменений текста!

Если в `SOLUTION` два пробела — ты обязан воспроизвести оба.
Если стоит запятая — ставь запятую, не точку.
Если там "π" — не заменяй на "пи".
Если есть буква "ё" — не превращай её в "е".
Если шаг содержит часть фразы в скобках, всё равно пиши её *ровно так, как в исходном тексте*, включая все пробелы, знаки и символы.

⚠️ Любое отклонение от точного текста `SOLUTION` (даже один символ) считается ошибкой.
Если сомневаешься — просто скопируй фрагмент из исходного текста без изменений.

---

### 🔍 Правило №2 — атомарность:
Каждый шаг должен содержать **только одно действие, утверждение или логический переход**.

---

### 📏 Где разделять:
Разделяй там, где:
- появляется новое вычисление, подстановка или преобразование;
- объявляется новая переменная;
- начинается новое рассуждение (“следовательно”, “значит”, “из этого получаем”);
- утверждается отдельный факт.

---

### 💬 Что можно опустить:
Игнорируй риторические вставки без смысловой нагрузки ("как видно", "далее", "теперь рассмотрим").

---

### 🧱 Формат вывода:
Пиши строго так:

```

1. "Первая точная подстрока из SOLUTION"
2. "Следующая точная подстрока из SOLUTION"
   ...

```

Только номер, точка и кавычки.
Без пояснений, перевода строк, комментариев и других символов.

---

### 📘 Пример:

Исходное решение:
"Площадь треугольника равна половине произведения основания на высоту. Основание равно 6, высота — 4. Следовательно, площадь равна (6 * 4) / 2 = 12."

Ожидаемый результат:
1. "Площадь треугольника равна половине произведения основания на высоту."
2. "Основание равно 6,"
3. "высота — 4."
4. "Следовательно, площадь равна (6 * 4) / 2 = 12."

---

Теперь разложи следующий текст решения на такие шаги:

{SOLUTION}
'''


PROMPT_MARK_ERRORS_TOKEN_WITH_REFERENCE = r"""
Ниже дан текст задачи, эталонное (правильное) решение и нумерованный список пошаговых рассуждений, которые нужно проверить.

Твоя основная задача: сверяясь с эталонным решением и условием задачи, для каждого шага из проверяемого решения найти все объективно неверные элементы (фактические, математические, арифметические, логические, неверные направления неравенств, некорректные преобразования и т.п.) и выделить каждую **минимальную** ошибочную подстроку специальными токенами [ERROR]…[ERROR].

ВАЖНО — СТРОГО: перед началом анализа у тебя есть:
- формулировка задачи (условие);
- эталонное правильное решение (может содержать LaTeX);
- проверяемое решение (тоже пронумерованный список шагов), которое может содержать ошибки.

ОБЯЗАТЕЛЬНЫЕ проверки:
A) Всегда сверяй содержание каждого шага проверяемого решения сначала с соответствующим шагом эталона (по номеру). Если нумерация отличается, попытайся логически сопоставить шаги по смыслу; если сопоставление невозможно — используй эталон для глобальной проверки фактов/выводов, и помечай всё, что явно противоречит эталону или условию задачи.

B) Для любых числовых выражений внутри шага:
   - вычисли их численно (точно или приближённо с абсолютной погрешностью ≤ 1e-6);
   - сравни вычисленный результат с тем, что написано в шаге. Если шаг содержит '≈', используй относительную погрешность 1e-4;
   - если расхождение больше допустимой погрешности — пометь **написанное в шаге** как [ERROR]... [ERROR].

C) Если шаг содержит одно или несколько выражений со знаком «=», проверяй **только итоговые значения**, т.е. те, что находятся **после последнего знака "="** в строке.
   - Все промежуточные вычисления и частичные равенства (до последнего "=") считаются частью рассуждения и **не должны помечаться как ошибки**, даже если в них есть неточности, округления или опечатки.
   - Проверка проводится только для финального результата — значения, стоящего после последнего знака "=" (или после "≈", если он заключает итог).
   - Если итоговое значение не совпадает с эталоном с учётом допустимой погрешности, то помечай **только это число или выражение после последнего "="** как [ERROR]... [ERROR].
   - Если в строке есть несколько независимых выражений (разделённых точкой или точкой с запятой), правило "только после последнего '='" применяется **к каждой логической части отдельно**
D) Если шаг содержит алгебраическое преобразование (A → B), проверь эквивалентность подстановкой 3 различных числовых наборов для переменных; при несоответствии — пометь минимальную фрагментную подстроку, где операция записана неверно.
   Если же шаг содержит несколько "=" подряд (цепочку вычислений), сравнение проводится только по последнему значению в цепочке.

E) Если проверяемый шаг утверждает результат, который в явном виде расходится с выводом эталона — выделяй ту подстроку, где указано неверное число/выражение/утверждение.

F) Если эталон даёт более подробный вывод, и проверяемый шаг опускает шаги, которые приводят к ошибке позже — помечай как ошибку конкретное неверное числовое/алгебраическое утверждение в проверяемом шаге (не помечай просто «отсутствие вывода»).

СТРОГИЕ ФОРМАЛЬНЫЕ ПРАВИЛА МАРКИРОВКИ:
1) Для каждой обнаруженной ошибки выделяй **минимальную подстроку** в шаге, которая является ошибкой. Минимальная подстрока — одно число/выражение/операция или 1–3 слова, точно совпадающие с оригиналом (символ-в-символ).
2) Вставляй `[ERROR]` **перед началом** этой подстроки и `[ERROR]` **сразу после** её конца. Пример: `2 + 2 = [ERROR]5[ERROR]`. Не добавляй пробелы внутрь/вокруг маркеров относительно выделенной подстроки.
3) Если в одном шаге несколько разных ошибок — выдели их **все**, каждая своими маркерами. Если одна и та же ошибочная подстрока встречается несколько раз **в одном шаге**, выдели её **только один раз** в этом шаге. Если та же ошибочная подстрока встречается в последующих шагах — **выделяй её каждый раз**.
4) Подстроки должны быть точными фрагментами исходного шага (символ-в-символ). Не заменяй текст и не рефакторь выражения — только вставляй маркеры.
5) Если в шаге **нет** объективных ошибок (с учётом эталона и вычислительных проверок) — выведи шаг **без изменений** (с той же нумерацией).
6) Формат выхода: ровно столько строк, сколько входных шагов в <steps>, в том же порядке и с той же нумерацией. **Только** эти строки — ничего лишнего.
7) Обрабатывай LaTeX/математику как текст: если ошибка внутри формулы, вставляй маркеры внутри формулы в соответствующем месте.
8) При сомнении помечай только то, что **однозначно** неверно относительно эталона/условия/строгой арифметики. Не помечай корректные варианты вывода, возможные эквивалентные преобразования или стильные отличия.

ДОПОЛНИТЕЛЬНЫЕ УТИЛИТАРНЫЕ ПОЛОЖЕНИЯ (для точности):
- Если эталон явно содержит вычисление (например показывает `LHS = <число>`), используй этот числовой эталон при сравнении; если эталон даёт выражение, вычисляй выражение и сверяй.
- Если в <steps> есть несколько утверждений, которые противоречат сразу нескольким шагам эталона, помечай минимальные подстроки в каждом конкретном утверждении (не объединяй ошибки в одну большую метку).
- Не добавляй в выход объяснений или причин — только исходный шаг с маркерами.

---

Вот тебе пример того, как выполнить задачу:

Условие задачи:

<task>
При каком натуральном $K$ величина $\\frac{n^2}{1.001^n}$ достигает максимального значения? Ответ запишите в виде десятичной дроби, округлив по правилам математического округления до 3 знаков после запятой.
</task>

Эталонное решение

<reference>
1. Для решения задачи необходимо найти натуральное число $ K $, при котором величина $\frac{n^2}{1.001^n}$ достигает максимального значения.
2. Далее, ответ должен быть записан в виде десятичной дроби, округленной до трёх знаков после запятой.
3. ### Анализ задачи
4. 1. Рассмотрим функцию $ f(x) = \frac{x^2}{1.001^x} $ для непрерывной переменной $ x $. Чтобы найти точку максимума, можно использовать метод дифференцирования или анализ отношения $ \frac{f(n+1)}{f(n)} $.
5. 2. Для анализа последовательности $ a_n = \frac{n^2}{1.001^n} $ рассмотрим отношение $ \frac{a_{n+1}}{a_n} $:
6. $$\n\frac{a_{n+1}}{a_n} = \frac{(n+1)^2}{n^2} \cdot \frac{1}{1.001} = \left(1 + \frac{2}{n} + \frac{1}{n^2}\right) \cdot \frac{1}{1.001}.\n$$
7.   При $ n = 2000 $: $ \frac{a_{2001}}{a_{2000}} > 1 $, значит, последовательность растёт.
8.   При $ n = 2001 $: $ \frac{a_{2002}}{a_{2001}} < 1 $, значит, последовательность убывает.
9. 3. Таким образом, максимальное значение достигается при $ n = 2001 $.
10. 4. Для точного определения точки максимума непрерывной функции $ f(x) $, используем производную. Точка экстремума находится в $ x = \frac{2}{\ln(1.001)} $. Подсчёт показывает, что эта точка приближается к $ x \approx 2000.9998 $.
11. 5. Округление $ x \approx 2000.9998 $ до трёх знаков после запятой даёт $ 2001.000 $.
12. ### Итоговый ответ
13. В задаче указано, что ответ должен быть записан в виде десятичной дроби. Учитывая, что точка максимума непрерывной функции приближается к $ 2000.9998 $, округлённая до трёх знаков после запятой, это $ 2001.000 $.
14. $$\n\boxed{2001.000}\n$$
</reference>

Проверяемые рассуждения:

<steps>
1. Есть последовательность a_n = n^2 / 1.001^n.
2. Чтобы понять, где она достигает пика, проще всего глянуть на соседние члены: возьмём отношение r_n = a_{n+1} / a_n.
3. Оно получается таким: r_n = ((n+1)^2 / n^2) / 1.001 = (1 + 1/n)^2 / 1.001.
4. Если r_n ≥ 1 — значит, растём, если < 1 — пошло убывание.
5. Теперь надо понять, при каких n выполняется (1 + 1/n)^2 ≥ 1.001.
6. Это то же, что 1 + 1/n ≥ √1.001.
7. Оценю корень на глаз по формуле для √(1+ε): при ε = 0.001 получается примерно 1 + ε/2 + ε^2/8 ≈ 1 + 0.0005 + 0.000000125 = 1.000500125.
8. Тогда 1 + 1/n ≥ 1.000500125, то есть 1/n ≥ 0.000500125, а значит n ≲ 1/0.000500125 ≈ 1999.5.
9. Иначе говоря, переключение где-то между 1999 и 2000, так что имеет смысл проверить границу вручную.
10. Считаю по месту: — Для n = 1999: (1 + 1/1999)^2 ≈ 1 + 2/1999 ≈ 1.0010005 (крошечный квадрат можно не учитывать).
11. Тогда r_1999 ≈ 1.0010005 / 1.001 ≈ 1.0000005, то есть ещё идём вверх.
12. — Для n = 2000: (1 + 1/2000)^2 = (1.0005)^2 ≈ 1.00050025.
13. Делим на 1.001: r_2000 ≈ 1.00050025 / 1.001 ≈ 0.9995, то есть дальше начинается спад.
14. Значит максимум как раз ловится на границе: при K = 2000.
15. Окончательный ответ (в виде десятичной дроби, с округлением до 3 знаков): 2000.000
</steps>

Вывод:
1. Есть последовательность a_n = n^2 / 1.001^n.
2. Чтобы понять, где она достигает пика, проще всего глянуть на соседние члены: возьмём отношение r_n = a_{n+1} / a_n.
3. Оно получается таким: r_n = ((n+1)^2 / n^2) / 1.001 = (1 + 1/n)^2 / 1.001.
4. Если r_n ≥ 1 — значит, растём, если < 1 — пошло убывание.
5. Теперь надо понять, при каких n выполняется (1 + 1/n)^2 ≥ 1.001.
6. Это то же, что 1 + 1/n ≥ √1.001.
7. Оценю корень на глаз по формуле для √(1+ε): при ε = 0.001 получается примерно 1 + ε/2 + [ERROR]ε^2/8[ERROR] ≈ 1 + 0.0005 + 0.000000125 = [ERROR]1.000500125[ERROR].
8. Тогда 1 + 1/n ≥ [ERROR]1.000500125[ERROR], то есть 1/n ≥ [ERROR]0.000500125[ERROR], а значит n ≲ [ERROR]1/0.000500125[ERROR] ≈ [ERROR]1999.5[ERROR].
9. Иначе говоря, переключение где-то между [ERROR]1999 и 2000[ERROR], так что имеет смысл проверить границу вручную.
10. Считаю по месту: — Для n = 1999: (1 + 1/1999)^2 ≈ 1 + 2/1999 ≈ 1.0010005 (крошечный квадрат можно не учитывать).
11. Тогда r_1999 ≈ 1.0010005 / 1.001 ≈ 1.0000005, то есть ещё идём вверх.
12. — Для n = 2000: (1 + 1/2000)^2 = (1.0005)^2 ≈ [ERROR]1.00050025[ERROR].
13. Делим на 1.001: r_2000 ≈ [ERROR]1.00050025[ERROR] / 1.001 ≈ [ERROR]0.9995[ERROR], то есть дальше начинается [ERROR]спад[ERROR].
14. Значит максимум как раз ловится на границе: при K = 2000.

Пример номер 2 на доказательства:

<task>
'Докажите: если \\(\\varphi\\) выпукла на \\([a,b]\\), то \\(\\varphi\\) непрерывна на \\([a,b]\\).'
<task>


Проверяемые рассуждения:

<steps>
1. Хотим понять, почему любая выпуклая функция φ на отрезке [a,b] не рвется: значения не прыгают ни внутри, ни у концов. Интуитивно выпуклость запрещает резкие «пики», поэтому график ведет себя спокойно.
2. Начнем с простой геометрии на хорде. Возьмем любую внутреннюю точку x0 и два числа y < x0 < z из [a,b].
3. Для любого x между y и z можно представить его как x = λ y + (1−λ) z, где удобно взять λ = (x − y)/(z − y).
4. Тогда по выпуклости имеем оценку снизу: φ(x) ≥ λ φ(y) + (1−λ) φ(z).
5. Из этого сразу видно, что φ(x) не может сильно проваливаться: φ(x) ≥ min{φ(y), φ(z)}.
6. То есть в окрестности x0 функция локально ограничена снизу;
7. симметричная оценка по другой хорде дает и контроль сверху, если нужно.
8. Теперь про предельные переходы. Хотим сравнить значение в точке с пределами вдоль последовательностей.
9. Пусть x_n → x0. Удобно записать x0 как выпуклую смесь x_n и отраженной точки 2x0 − x_n:
10. x0 = t x_n + (1−t)(2x0 − x_n), где t ∈ (0,1) фиксировано.
11. По выпуклости получаем оценку φ(x0) ≤ t φ(x_n) + φ(2x0 − x_n).
12. При больших n обе точки x_n и 2x0 − x_n сидят внутри выбранного (y,z), так что φ(2x0 − x_n) ≤ M для некоторой константы M, зависящей только от y,z.
13. Переходя к liminf по n, имеем φ(x0) ≤ t liminf φ(x_n) + M.
14. Пусть теперь t → 1. Тогда получается φ(x0) ≤ liminf φ(x_n) + M.
15. То есть значений ниже предела не бывает «сильно», и эпиграф в этом смысле ведет себя замкнуто.
16. Чтобы получить и контроль сверху (а это и есть нужная непрерывность), рассмотрим последовательность x_n → x0, у которой бесконечно много членов лежит справа от x0.
17. Для таких n можно записать x_n = α_n x0 + (1−α_n) z, где α_n = (z − x0)/(z − x_n) и при n → ∞ имеем α_n → 0.
18. Применяем выпуклость к этой записи: φ(x_n) ≤ α_n φ(x0) + (1−α_n) φ(z) + φ(y).
19. Переходя к limsup, получаем limsup φ(x_n) ≤ φ(y).
20. А так как y можно взять сколь угодно близко к x0, из этого в итоге вытекает limsup φ(x_n) ≤ φ(x0).
21. Аналогично разбирается подпоследовательность слева.
22. Значит, сверху скачка тоже нет.
23. Остается про концы. Для левого конца a берем фиксированное z ∈ (a,b] и пишем для x → a+:
24. x = α a + (1−α) z, где α = (z − x)/(z − a).
25. Тогда по выпуклости φ(x) ≤ α φ(a) + (1−α) φ(z).
26. Поскольку при x → a+ имеем α → 0, правая часть стремится к φ(a), и, следовательно, limsup_{x→a+} φ(x) ≤ φ(a).
27. С другой стороны, из рассуждений с liminf выше имеем φ(a) ≤ liminf_{x→a+} φ(x).
28. Отсюда сразу lim_{x→a+} φ(x) = φ(a).
29. Правый конец b рассматривается так же, только берем фиксированное y ∈ [a,b) и предел слева: получаем lim_{x→b−} φ(x) = φ(b).
30. В итоге: – внутри (a,b) функция не может ни проваливаться вниз, ни выскакивать вверх, потому что ее значения «зажаты» выпуклыми комбинациями соседних точек;
31. – на концах односторонние пределы совпадают со значениями;
32. значит, φ непрерывна на всем [a,b].
33. Дополнительно, в качестве альтернативы можно смотреть на односторонние производные: у выпуклой функции они существуют в каждой внутренней точке и монотонно меняются, что автоматически дает равномерную оценку по Липшицу на любом подотрезке и ту же самую непрерывность.
</steps>

Вывод:
1. Хотим понять, почему любая выпуклая функция φ на отрезке [a,b] не рвется: значения не прыгают ни внутри, ни у концов. Интуитивно выпуклость запрещает резкие «пики», поэтому график ведет себя спокойно.
2. Начнем с простой геометрии на хорде. Возьмем любую внутреннюю точку x0 и два числа y < x0 < z из [a,b]. 
3. Для любого x между y и z можно представить его как x = λ y + (1−λ) z, где удобно взять λ = (x − y)/(z − y). 
4. Тогда по выпуклости имеем [ERROR]оценку снизу[ERROR]:[ERROR]φ(x) ≥ λ φ(y) + (1−λ) φ(z)[ERROR].
5. Из этого сразу видно, что φ(x) не может сильно проваливаться: [ERROR]φ(x) ≥ min{φ(y), φ(z)}[ERROR]. 
6. То есть в окрестности x0 функция [ERROR]локально ограничена снизу[ERROR]; 
7. [ERROR]симметричная оценка по другой хорде дает и контроль сверху[ERROR], если нужно.
8. Теперь про предельные переходы. Хотим сравнить значение в точке с пределами вдоль последовательностей. 
9. Пусть x_n → x0. Удобно записать x0 как выпуклую смесь x_n и отраженной точки 2x0 − x_n:
10. x0 = t x_n + (1−t)(2x0 − x_n), где t ∈ (0,1) фиксировано.
11. По выпуклости получаем оценку[ERROR]φ(x0) ≤ t φ(x_n) + φ(2x0 − x_n)[ERROR].
12. При больших n обе точки x_n и 2x0 − x_n сидят внутри выбранного (y,z), так что φ(2x0 − x_n) ≤ M для некоторой константы M, зависящей только от y,z. 
13. Переходя к liminf по n, имеем[ERROR]φ(x0) ≤ t liminf φ(x_n) + M[ERROR].
14. Пусть теперь t → 1. Тогда получается [ERROR]φ(x0) ≤ liminf φ(x_n) + M[ERROR]. 
15. То есть [ERROR]значений ниже предела не бывает «сильно»[ERROR], и [ERROR]эпиграф в этом смысле ведет себя замкнуто[ERROR].
16. Чтобы получить и контроль сверху (а это и есть нужная непрерывность), рассмотрим последовательность x_n → x0, у которой бесконечно много членов лежит справа от x0. 
17. Для таких n можно записатьx_n = α_n x0 + (1−α_n) z, где [ERROR]α_n = (z − x0)/(z − x_n)[ERROR] и при n → ∞ имеем [ERROR]α_n → 0[ERROR].
18. Применяем выпуклость к этой записи:φ(x_n) ≤ α_n φ(x0) + (1−α_n) φ(z)[ERROR] + φ(y)[ERROR].
19. Переходя к limsup, получаем [ERROR]limsup φ(x_n) ≤ φ(y)[ERROR]. 
20. А так как y можно взять сколь угодно близко к x0, из этого в итоге [ERROR]вытекает[ERROR] limsup φ(x_n) ≤ φ(x0). 
21. Аналогично разбирается подпоследовательность слева. 
22. Значит, сверху скачка тоже нет.
23. Остается про концы. Для левого конца a берем фиксированное z ∈ (a,b] и пишем для x → a+:
24. x = α a + (1−α) z, где α = (z − x)/(z − a).
25. Тогда по выпуклостиφ(x) ≤ α φ(a) + (1−α) φ(z).
26. Поскольку при x → a+ имеем [ERROR]α → 0[ERROR], [ERROR]правая часть стремится к φ(a)[ERROR], и, следовательно, limsup_{x→a+} φ(x) ≤ φ(a). 
27. С другой стороны, [ERROR]из рассуждений с liminf выше имеем[ERROR] φ(a) ≤ liminf_{x→a+} φ(x). 
28. Отсюда сразу lim_{x→a+} φ(x) = φ(a). 
29. Правый конец b рассматривается так же, только берем фиксированное y ∈ [a,b) и предел слева: получаем lim_{x→b−} φ(x) = φ(b).
30. В итоге:– внутри (a,b) функция не может ни проваливаться вниз, ни выскакивать вверх, потому что ее значения «зажаты» выпуклыми комбинациями соседних точек;
31. – на концах односторонние пределы совпадают со значениями;
32. значит, φ непрерывна на всем [a,b]. 
33. Дополнительно, в качестве альтернативы можно смотреть на односторонние производные: у выпуклой функции они существуют в каждой внутренней точке и монотонно меняются, что автоматически дает равномерную оценку по Липшицу на любом подотрезке и ту же самую непрерывность.


Дополнительно: если ошибочное число или выражение встречается в **нескольких шагах** после первого обнаружения, его нужно включить во все эти шаги.

Теперь обработай рассуждения, сверяясь с задачей и эталонным решением при необходимости, и выведи результат **точно** в указанном формате — ничего больше.

---

ВХОДНЫЕ ДАННЫЕ:

Условие задачи:

<task>
{TASK}
</task>

Эталонное решение:

<reference>
{REFERENCE}
</reference>

Проверяемые рассуждения:

<steps>
{STEPS}
</steps>
"""

# SELF_CORRECTION = r"""
# # Ниже дан текст задачи, эталонное (правильное) решение и нумерованный список шагов проверяемого решения, в котором уже могут быть частично или полностью размечены ошибки с помощью токенов [ERROR]…[ERROR].
#
# # Твоя задача — ДОПРОВЕРИТЬ решение:
# # 1. Сверяясь с эталонным решением и условием задачи, найди все ошибки, которые могли остаться неразмеченными.
# # 2. Добавь недостающие [ERROR]…[ERROR] только там, где это действительно нужно.
# # 3. Уже существующие [ERROR]-метки не удаляй, не изменяй и не переставляй.
# # 4. Не размечай заново шаги, которые уже полностью покрыты корректными маркерами — просто оставь их без изменений.
#
# # Что считать ошибкой:
# # - фактические, математические, арифметические, логические ошибки;
# # - неверные знаки неравенств, некорректные преобразования, противоречия с эталоном или условием;
# # - неверные вычисления, если результат расходится с эталоном (или с точным вычислением) больше, чем на 1e-6 (при "≈" — 1e-4);
# # - неверный итог после последнего знака "=" (сравни только финальные значения);
# # - при алгебраических преобразованиях проверяй эквивалентность подстановкой 3 разных наборов значений.
#
# # Важно:
# # - Ошибка выделяется **минимальной подстрокой**, строго символ-в-символ из исходного шага.
# # - Формат выделения: [ERROR]ошибочная_подстрока[ERROR].
# # - Не добавляй пробелы внутрь или вокруг маркеров.
# # - В одном шаге может быть несколько ошибок — выдели каждую отдельно.
# # - Если шаг без ошибок — выведи его как есть.
# # - Если шаг уже содержит частичные метки, только дополни их недостающими, не дублируй.
#
# # Формат вывода:
# # — тот же список шагов (с номерами и тем же порядком);
# # — никаких комментариев, объяснений или лишнего текста, только итоговые строки шагов;
# # — каждый шаг строго один к одному с входными номерами.
#
# # Если сомневаешься — помечай только то, что объективно неверно относительно эталона или арифметики.
# # ---
#
# # Вот тебе пример того, как выполнить задачу:
#
# # Условие задачи:
#
# # <task>
# # При каком натуральном $K$ величина $\\frac{n^2}{1.001^n}$ достигает максимального значения? Ответ запишите в виде десятичной дроби, округлив по правилам математического округления до 3 знаков после запятой.
# # </task>
#
# # Эталонное решение
#
# # <reference>
# # 1. Для решения задачи необходимо найти натуральное число $ K $, при котором величина $\frac{n^2}{1.001^n}$ достигает максимального значения.
# # 2. Далее, ответ должен быть записан в виде десятичной дроби, округленной до трёх знаков после запятой.
# # 3. ### Анализ задачи
# # 4. 1. Рассмотрим функцию $ f(x) = \frac{x^2}{1.001^x} $ для непрерывной переменной $ x $. Чтобы найти точку максимума, можно использовать метод дифференцирования или анализ отношения $ \frac{f(n+1)}{f(n)} $.
# # 5. 2. Для анализа последовательности $ a_n = \frac{n^2}{1.001^n} $ рассмотрим отношение $ \frac{a_{n+1}}{a_n} $:
# # 6. $$\n\frac{a_{n+1}}{a_n} = \frac{(n+1)^2}{n^2} \cdot \frac{1}{1.001} = \left(1 + \frac{2}{n} + \frac{1}{n^2}\right) \cdot \frac{1}{1.001}.\n$$
# # 7.   При $ n = 2000 $: $ \frac{a_{2001}}{a_{2000}} > 1 $, значит, последовательность растёт.
# # 8.   При $ n = 2001 $: $ \frac{a_{2002}}{a_{2001}} < 1 $, значит, последовательность убывает.
# # 9. 3. Таким образом, максимальное значение достигается при $ n = 2001 $.
# # 10. 4. Для точного определения точки максимума непрерывной функции $ f(x) $, используем производную. Точка экстремума находится в $ x = \frac{2}{\ln(1.001)} $. Подсчёт показывает, что эта точка приближается к $ x \approx 2000.9998 $.
# # 11. 5. Округление $ x \approx 2000.9998 $ до трёх знаков после запятой даёт $ 2001.000 $.
# # 12. ### Итоговый ответ
# # 13. В задаче указано, что ответ должен быть записан в виде десятичной дроби. Учитывая, что точка максимума непрерывной функции приближается к $ 2000.9998 $, округлённая до трёх знаков после запятой, это $ 2001.000 $.
# # 14. $$\n\boxed{2001.000}\n$$
# # </reference>
#
# # Проверяемые рассуждения:
#
# # <steps>
# # 1. Есть последовательность a_n = n^2 / 1.001^n.
# # 2. Чтобы понять, где она достигает пика, проще всего глянуть на соседние члены: возьмём отношение r_n = a_{n+1} / a_n.
# # 3. Оно получается таким: r_n = ((n+1)^2 / n^2) / 1.001 = (1 + 1/n)^2 / 1.001.
# # 4. Если r_n ≥ 1 — значит, растём, если < 1 — пошло убывание.
# # 5. Теперь надо понять, при каких n выполняется (1 + 1/n)^2 ≥ 1.001.
# # 6. Это то же, что 1 + 1/n ≥ √1.001.
# # 7. Оценю корень на глаз по формуле для √(1+ε): при ε = 0.001 получается примерно 1 + ε/2 + ε^2/8 ≈ 1 + 0.0005 + 0.000000125 = 1.000500125.
# # 8. Тогда 1 + 1/n ≥ [ERROR]1.000500125[ERROR], то есть 1/n ≥ 0.000500125, а значит n ≲ 1/0.000500125 ≈ 1999.5.
# # 9. Иначе говоря, переключение где-то между 1999 и 2000, так что имеет смысл проверить границу вручную.
# # 10. Считаю по месту: — Для n = 1999: (1 + 1/1999)^2 ≈ 1 + 2/1999 ≈ 1.0010005 (крошечный квадрат можно не учитывать).
# # 11. Тогда r_1999 ≈ 1.0010005 / 1.001 ≈ 1.0000005, то есть ещё идём вверх.
# # 12. — Для n = 2000: (1 + 1/2000)^2 = (1.0005)^2 ≈ 1.00050025.
# # 13. Делим на 1.001: r_2000 ≈ 1.00050025 / 1.001 ≈ 0.9995, то есть дальше начинается спад.
# # 14. Значит максимум как раз ловится на границе: при K = 2000.
# # 15. Окончательный ответ (в виде десятичной дроби, с округлением до 3 знаков): 2000.000
# # </steps>
#
# # Вывод:
# # 1. Есть последовательность a_n = n^2 / 1.001^n.
# # 2. Чтобы понять, где она достигает пика, проще всего глянуть на соседние члены: возьмём отношение r_n = a_{n+1} / a_n.
# # 3. Оно получается таким: r_n = ((n+1)^2 / n^2) / 1.001 = (1 + 1/n)^2 / 1.001.
# # 4. Если r_n ≥ 1 — значит, растём, если < 1 — пошло убывание.
# # 5. Теперь надо понять, при каких n выполняется (1 + 1/n)^2 ≥ 1.001.
# # 6. Это то же, что 1 + 1/n ≥ √1.001.
# # 7. Оценю корень на глаз по формуле для √(1+ε): при ε = 0.001 получается примерно 1 + ε/2 + [ERROR]ε^2/8[ERROR] ≈ 1 + 0.0005 + 0.000000125 = [ERROR]1.000500125[ERROR].
# # 8. Тогда 1 + 1/n ≥ [ERROR]1.000500125[ERROR], то есть 1/n ≥ [ERROR]0.000500125[ERROR], а значит n ≲ [ERROR]1/0.000500125[ERROR] ≈ [ERROR]1999.5[ERROR].
# # 9. Иначе говоря, переключение где-то между [ERROR]1999 и 2000[ERROR], так что имеет смысл проверить границу вручную.
# # 10. Считаю по месту: — Для n = 1999: (1 + 1/1999)^2 ≈ 1 + 2/1999 ≈ 1.0010005 (крошечный квадрат можно не учитывать).
# # 11. Тогда r_1999 ≈ 1.0010005 / 1.001 ≈ 1.0000005, то есть ещё идём вверх.
# # 12. — Для n = 2000: (1 + 1/2000)^2 = (1.0005)^2 ≈ [ERROR]1.00050025[ERROR].
# # 13. Делим на 1.001: r_2000 ≈ [ERROR]1.00050025[ERROR] / 1.001 ≈ [ERROR]0.9995[ERROR], то есть дальше начинается [ERROR]спад[ERROR].
# # 14. Значит максимум как раз ловится на границе: при K = 2000.
#
# Пример номер 2 на доказательства:
#
# # <task>
# # 'Докажите: если \\(\\varphi\\) выпукла на \\([a,b]\\), то \\(\\varphi\\) непрерывна на \\([a,b]\\).'
# # <task>
#
#
# # Проверяемые рассуждения:
#
# # <steps>
# # 1. Хотим понять, почему любая выпуклая функция φ на отрезке [a,b] не рвется: значения не прыгают ни внутри, ни у концов. Интуитивно выпуклость запрещает резкие «пики», поэтому график ведет себя спокойно.
# # 2. Начнем с простой геометрии на хорде. Возьмем любую внутреннюю точку x0 и два числа y < x0 < z из [a,b].
# # 3. Для любого x между y и z можно представить его как x = λ y + (1−λ) z, где удобно взять λ = (x − y)/(z − y).
# # 4. Тогда по выпуклости имеем [ERROR]оценку снизу[ERROR]:[ERROR]φ(x) ≥ λ φ(y) + (1−λ) φ(z)[ERROR].
# # 5. Из этого сразу видно, что φ(x) не может сильно проваливаться: [ERROR]φ(x) ≥ min{φ(y), φ(z)}[ERROR].
# # 6. То есть в окрестности x0 функция локально ограничена снизу;
# # 7. симметричная оценка по другой хорде дает и контроль сверху, если нужно.
# # 8. Теперь про предельные переходы. Хотим сравнить значение в точке с пределами вдоль последовательностей.
# # 9. Пусть x_n → x0. Удобно записать x0 как выпуклую смесь x_n и отраженной точки 2x0 − x_n:
# # 10. x0 = t x_n + (1−t)(2x0 − x_n), где t ∈ (0,1) фиксировано.
# # 11. По выпуклости получаем оценку φ(x0) ≤ t φ(x_n) + φ(2x0 − x_n).
# # 12. При больших n обе точки x_n и 2x0 − x_n сидят внутри выбранного (y,z), так что φ(2x0 − x_n) ≤ M для некоторой константы M, зависящей только от y,z.
# # 13. Переходя к liminf по n, имеем φ(x0) ≤ t liminf φ(x_n) + M.
# # 14. Пусть теперь t → 1. Тогда получается φ(x0) ≤ liminf φ(x_n) + M.
# # 15. То есть значений ниже предела не бывает «сильно», и эпиграф в этом смысле ведет себя замкнуто.
# # 16. Чтобы получить и контроль сверху (а это и есть нужная непрерывность), рассмотрим последовательность x_n → x0, у которой бесконечно много членов лежит справа от x0.
# # 17. Для таких n можно записать x_n = α_n x0 + (1−α_n) z, где α_n = (z − x0)/(z − x_n) и при n → ∞ имеем α_n → 0.
# # 18. Применяем выпуклость к этой записи: φ(x_n) ≤ α_n φ(x0) + (1−α_n) φ(z) + φ(y).
# # 19. Переходя к limsup, получаем limsup φ(x_n) ≤ φ(y).
# # 20. А так как y можно взять сколь угодно близко к x0, из этого в итоге вытекает limsup φ(x_n) ≤ φ(x0).
# # 21. Аналогично разбирается подпоследовательность слева.
# # 22. Значит, сверху скачка тоже нет.
# # 23. Остается про концы. Для левого конца a берем фиксированное z ∈ (a,b] и пишем для x → a+:
# # 24. x = α a + (1−α) z, где α = (z − x)/(z − a).
# # 25. Тогда по выпуклости φ(x) ≤ α φ(a) + (1−α) φ(z).
# # 26. Поскольку при x → a+ имеем α → 0, правая часть стремится к φ(a), и, следовательно, limsup_{x→a+} φ(x) ≤ φ(a).
# # 27. С другой стороны, из рассуждений с liminf выше имеем φ(a) ≤ liminf_{x→a+} φ(x).
# # 28. Отсюда сразу lim_{x→a+} φ(x) = φ(a).
# # 29. Правый конец b рассматривается так же, только берем фиксированное y ∈ [a,b) и предел слева: получаем lim_{x→b−} φ(x) = φ(b).
# # 30. В итоге: – внутри (a,b) функция не может ни проваливаться вниз, ни выскакивать вверх, потому что ее значения «зажаты» выпуклыми комбинациями соседних точек;
# # 31. – на концах односторонние пределы совпадают со значениями;
# # 32. значит, φ непрерывна на всем [a,b].
# # 33. Дополнительно, в качестве альтернативы можно смотреть на односторонние производные: у выпуклой функции они существуют в каждой внутренней точке и монотонно меняются, что автоматически дает равномерную оценку по Липшицу на любом подотрезке и ту же самую непрерывность.
# # </steps>
#
# # Вывод:
# # 1. Хотим понять, почему любая выпуклая функция φ на отрезке [a,b] не рвется: значения не прыгают ни внутри, ни у концов. Интуитивно выпуклость запрещает резкие «пики», поэтому график ведет себя спокойно.
# # 2. Начнем с простой геометрии на хорде. Возьмем любую внутреннюю точку x0 и два числа y < x0 < z из [a,b].
# # 3. Для любого x между y и z можно представить его как x = λ y + (1−λ) z, где удобно взять λ = (x − y)/(z − y).
# # 4. Тогда по выпуклости имеем [ERROR]оценку снизу[ERROR]:[ERROR]φ(x) ≥ λ φ(y) + (1−λ) φ(z)[ERROR].
# # 5. Из этого сразу видно, что φ(x) не может сильно проваливаться: [ERROR]φ(x) ≥ min{φ(y), φ(z)}[ERROR].
# # 6. То есть в окрестности x0 функция [ERROR]локально ограничена снизу[ERROR];
# # 7. [ERROR]симметричная оценка по другой хорде дает и контроль сверху[ERROR], если нужно.
# # 8. Теперь про предельные переходы. Хотим сравнить значение в точке с пределами вдоль последовательностей.
# # 9. Пусть x_n → x0. Удобно записать x0 как выпуклую смесь x_n и отраженной точки 2x0 − x_n:
# # 10. x0 = t x_n + (1−t)(2x0 − x_n), где t ∈ (0,1) фиксировано.
# # 11. По выпуклости получаем оценку[ERROR]φ(x0) ≤ t φ(x_n) + φ(2x0 − x_n)[ERROR].
# # 12. При больших n обе точки x_n и 2x0 − x_n сидят внутри выбранного (y,z), так что φ(2x0 − x_n) ≤ M для некоторой константы M, зависящей только от y,z.
# # 13. Переходя к liminf по n, имеем[ERROR]φ(x0) ≤ t liminf φ(x_n) + M[ERROR].
# # 14. Пусть теперь t → 1. Тогда получается [ERROR]φ(x0) ≤ liminf φ(x_n) + M[ERROR].
# # 15. То есть [ERROR]значений ниже предела не бывает «сильно»[ERROR], и [ERROR]эпиграф в этом смысле ведет себя замкнуто[ERROR].
# # 16. Чтобы получить и контроль сверху (а это и есть нужная непрерывность), рассмотрим последовательность x_n → x0, у которой бесконечно много членов лежит справа от x0.
# # 17. Для таких n можно записатьx_n = α_n x0 + (1−α_n) z, где [ERROR]α_n = (z − x0)/(z − x_n)[ERROR] и при n → ∞ имеем [ERROR]α_n → 0[ERROR].
# # 18. Применяем выпуклость к этой записи:φ(x_n) ≤ α_n φ(x0) + (1−α_n) φ(z)[ERROR] + φ(y)[ERROR].
# # 19. Переходя к limsup, получаем [ERROR]limsup φ(x_n) ≤ φ(y)[ERROR].
# # 20. А так как y можно взять сколь угодно близко к x0, из этого в итоге [ERROR]вытекает[ERROR] limsup φ(x_n) ≤ φ(x0).
# # 21. Аналогично разбирается подпоследовательность слева.
# # 22. Значит, сверху скачка тоже нет.
# # 23. Остается про концы. Для левого конца a берем фиксированное z ∈ (a,b] и пишем для x → a+:
# # 24. x = α a + (1−α) z, где α = (z − x)/(z − a).
# # 25. Тогда по выпуклостиφ(x) ≤ α φ(a) + (1−α) φ(z).
# # 26. Поскольку при x → a+ имеем [ERROR]α → 0[ERROR], [ERROR]правая часть стремится к φ(a)[ERROR], и, следовательно, limsup_{x→a+} φ(x) ≤ φ(a).
# # 27. С другой стороны, [ERROR]из рассуждений с liminf выше имеем[ERROR] φ(a) ≤ liminf_{x→a+} φ(x).
# # 28. Отсюда сразу lim_{x→a+} φ(x) = φ(a).
# # 29. Правый конец b рассматривается так же, только берем фиксированное y ∈ [a,b) и предел слева: получаем lim_{x→b−} φ(x) = φ(b).
# # 30. В итоге:– внутри (a,b) функция не может ни проваливаться вниз, ни выскакивать вверх, потому что ее значения «зажаты» выпуклыми комбинациями соседних точек;
# # 31. – на концах односторонние пределы совпадают со значениями;
# # 32. значит, φ непрерывна на всем [a,b].
# # 33. Дополнительно, в качестве альтернативы можно смотреть на односторонние производные: у выпуклой функции они существуют в каждой внутренней точке и монотонно меняются, что автоматически дает равномерную оценку по Липшицу на любом подотрезке и ту же самую непрерывность.
#
#
# # Дополнительно: если ошибочное число или выражение встречается в **нескольких шагах** после первого обнаружения, его нужно включить во все эти шаги.
#
# # Теперь обработай рассуждения, сверяясь с задачей и эталонным решением при необходимости, и выведи результат **точно** в указанном формате — ничего больше.
#
# # ---
#
# # ВХОДНЫЕ ДАННЫЕ:
#
# # Условие задачи:
#
# # <task>
# # {TASK}
# # </task>
#
# # Эталонное решение:
#
# # <reference>
# # {REFERENCE}
# # </reference>
#
# # Проверяемые рассуждения:
#
# # <steps>
# # {STEPS}
# # </steps>
# # """

mark = '''
Give only one number from 1 to 3: the difficulty level of the task

Task:
TASK'''

hints = """
Ты — умный преподаватель, который помогает ученику понять свои ошибки, не раскрывая сразу правильное решение.

Ниже даны:
1️⃣ Условие задачи
2️⃣ Эталонное (правильное) решение
3️⃣ Неправильное решение ученика

Твоя задача — выдать РОВНО ТРИ ПОДСКАЗКИ, которые помогут ученику исправить ошибки, но не подскажут прямой ответ.

---

Формат вывода (строго соблюдать):

Подсказка 1
<текст первой подсказки>

Подсказка 2
<текст второй подсказки>

Подсказка 3
<текст третьей подсказки>

---

Требования:
- Подсказки должны быть осмысленными, шаг за шагом приближать ученика к правильному решению.
- Не раскрывай правильный ответ.
- Не пиши "перепроверь вычисления" — указывай, где конкретно стоит задуматься.
- Подсказки должны быть в возрастающем уровне точности: от общей наводки → к прямому направлению мысли.

---

Вводные данные:

Задача:
{task}

Правильное решение:
{correct_solution}

Неправильное решение ученика:
{wrong_solution}

---

Выведи только три подсказки строго в указанном формате.
"""


matching = r'''Ты — нейронная система оценки совпадений решений математических задач.  
Тебе дано два текста: условие задачи и предложенное решение.  
Твоя задача — оценить, насколько решение соответствует условию по 100-балльной шкале.

Правила:
- 100 — решение полностью соответствует условию задачи.
- 0 — решение абсолютно не соответствует.
- Оценивай строго по смыслу, не по стилю.
- Не пиши комментариев, объяснений или анализа.
- Ответь только одним числом от 0 до 100, без дополнительных символов.


Например:

Условие задачи:
Реши 2 + 2

Решение:
2 + 2 = 5

Твой ответ:
100 (потому что в условии и решении говориться об одной задаче)

ИЛИ

Условие задачи:
Реши 3 + 3

Решение:
4 + 4 = 8

Твой ответ:
0 (потому что в условии и решении говориться о абсолютно разных задачах)

---
Условие задачи:
TASK

Решение:
TEXT

---

Оцени степень соответствия решения условию (только число от 0 до 100):'''